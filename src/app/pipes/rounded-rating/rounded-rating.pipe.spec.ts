import { RoundedRatingPipe } from './rounded-rating.pipe';

describe('RoundedRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundedRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
